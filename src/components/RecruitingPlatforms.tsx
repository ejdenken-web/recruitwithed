import "./RecruitingPlatforms.css"; function RecruitingPlatforms() { const platforms = [ "LinkedIn Recruiter", "Indeed", "Dice", "Monster", "CareerBuilder", ]; return ( <section id="recruiting-platforms" className="recruiting-platforms-section" > <div className="recruiting-platforms-header"> <span className="section-tag">RECRUITING PLATFORMS</span> <p> Recruiting platforms used to source, identify, connect with, and engage qualified talent. </p> </div> <div className="platform-stage" aria-label="Recruiting platforms"> {platforms.map((platform, index) => ( <div key={platform} className={`moving-platform moving-platform-${index + 1}`} > <span className="platform-shadow" /> <span className="platform-spark" /> <span className="platform-name">{platform}</span> </div> ))} </div> </section> ); } export default RecruitingPlatforms;